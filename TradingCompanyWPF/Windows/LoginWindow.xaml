<Window x:Class="TradingCompany.WPF.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="System Login" Height="350" Width="300"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Grid Background="{StaticResource GrayColor}">
        <Border Background="White" CornerRadius="10" Margin="20" Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Welcome Back" FontSize="20" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#333"/>

                <TextBlock Text="Username" Foreground="#666"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Password" Foreground="#666" Margin="0,10,0,0"/>
                <PasswordBox x:Name="UserPasswordBox" />

                <Button Content="Login" 
                        Style="{StaticResource ModernBtn}" 
                        Command="{Binding LoginCommand}" 
                        CommandParameter="{Binding ElementName=UserPasswordBox}"
                        Margin="0,20,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>